/**
 * LEGACY ROUTE — Redirects to /api/payment/paypal/webhook/
 * Kept for backward compatibility with PayPal dashboard webhook config.
 */
export { POST, dynamic } from '@/app/api/payment/paypal/webhook/route';
