/**
 * LEGACY ROUTE — Redirects to /api/payment/paypal/capture/
 * Kept for backward compatibility with in-flight PayPal return URLs.
 */
export { GET, dynamic } from '@/app/api/payment/paypal/capture/route';
