# ── Database ──────────────────────────────────────────
# DB_PATH is set automatically by scripts/setup-production-db.js (Hostinger)
# or deploy/deploy.sh (VPS). For local dev, prisma/dev.db is used.
# DATABASE_URL is for Prisma CLI compatibility only (migrations).
DATABASE_URL="file:./dev.db"

# Admin JWT Secret (REQUIRED - change this in production!)
JWT_SECRET="ikfz-admin-jwt-secret-2026-change-in-production-kX9mP4qR7vB2nL5"